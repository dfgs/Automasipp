<UserControl x:Class="Automasipp.Desktop.Views.ArrowRightView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" x:Name="root"
             d:DesignWidth="800" Height="15" >
	<DockPanel VerticalAlignment="Center">
		<Path Width="15" DockPanel.Dock="Right" Stroke="{Binding ElementName=root,Path=Foreground}" Fill="{Binding ElementName=root,Path=Foreground}" Data="M0,0 L15,7.5 L0,15 Z" />
		<Rectangle Fill="{Binding ElementName=root,Path=Foreground}" Height="2" />
	</DockPanel>
</UserControl>
