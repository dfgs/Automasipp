<UserControl x:Class="Automasipp.Desktop.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Automasipp.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel>
		<TextBlock Text="Hostname:" FontWeight="Bold"/>
		<TextBox Text="{Binding Hostname}" Margin="5"/>
		<TextBlock Text="Port:" FontWeight="Bold"/>
		<TextBox Text="{Binding Port}" Margin="5"/>
		<Button Content="Connect" Width="150" Command="{Binding ConnectCommand}"/>
	</StackPanel>
</UserControl>
